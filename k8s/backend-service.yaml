apiVersion: v1
kind: Service
metadata:
  name: temperaturas-backend
spec:
  type: ClusterIP 
  selector:
    app: backend
  ports:
    - protocol: TCP
      port: 5000      # Puerto por el que el Front buscará al Back
      targetPort: 3005