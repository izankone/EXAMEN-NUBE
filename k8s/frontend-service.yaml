apiVersion: v1
kind: Service
metadata:
  name: frontend-service
spec:
  type: LoadBalancer 
  selector:
    app: frontend
  ports:
    - protocol: TCP
      port: 80       # Puerto que pondrás en el navegador
      targetPort: 3005 # El puerto interno de tu app